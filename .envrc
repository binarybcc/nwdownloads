# direnv configuration for NWDownloads project
# Automatically sets PROJECT_ROOT when entering this directory

# Auto-detect project root (current directory)
export PROJECT_ROOT=$(pwd)

# Optional: Add project bin directory to PATH if needed
# PATH_add bin

# Log that direnv loaded (helpful for debugging)
echo "✓ NWDownloads environment loaded (PROJECT_ROOT=$PROJECT_ROOT)"
