parameters:
    level: 5
    paths:
        - web
    excludePaths:
        - web/vendor (?)
        - web/*_example.php
        # CLI utility scripts (use $argv which isn't guaranteed in PHPStan context)
        - web/cleanup_bad_data.php
        - web/purge_all_data.php
    ignoreErrors: []
        # Previously ignored errors in rates.php have been fixed
        # No ignored error patterns needed as of 2025-12-16
