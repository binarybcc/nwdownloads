---
phase: 08-trend-expansion-log-retention
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - web/api/legacy.php
  - web/assets/js/core/app.js
  - web/assets/js/components/trend-slider.js
autonomous: true
requirements:
  - TREND-01

must_haves:
  truths:
    - "BU card mini-charts on the overview page display 13 data points instead of 12"
    - "Clicking a bar chart opens a trend slider that defaults to 13 weeks"
    - "The trend slider offers 4, 13, 26, 52 week buttons (12weeks replaced by 13weeks in UI)"
    - "Existing 12weeks URLs still resolve correctly (backward compatibility)"
  artifacts:
    - path: "web/api/legacy.php"
      provides: "13-week default in getBusinessUnitTrendData(), getHistoricalTrend(), $weeksMap"
      contains: "'13weeks' => 13"
    - path: "web/assets/js/core/app.js"
      provides: "13-Week Trend labels on BU cards"
      contains: "13-Week Trend"
    - path: "web/assets/js/components/trend-slider.js"
      provides: "13weeks default and button in trend slider"
      contains: "'13weeks'"
  key_links:
    - from: "web/assets/js/components/trend-slider.js"
      to: "web/api/legacy.php"
      via: "API call with time_range parameter"
      pattern: "13weeks"
    - from: "web/assets/js/core/app.js"
      to: "web/api/legacy.php getBusinessUnitTrendData()"
      via: "BU card data fetch"
      pattern: "13-Week Trend"
---

<objective>
Expand all trend views from 12-week to 13-week defaults (a full quarter) and add 13weeks as a selectable option in the trend slider. Update backend PHP functions, API defaults, and frontend JS labels/buttons.

Purpose: Users see a full quarter of trend data by default, providing better seasonal context for subscriber analysis.
Output: Modified legacy.php (backend defaults + weeksMap), app.js (labels), trend-slider.js (default + buttons).
</objective>

<execution_context>
@/Users/user/.claude/get-shit-done/workflows/execute-plan.md
@/Users/user/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-trend-expansion-log-retention/08-CONTEXT.md
@.planning/phases/08-trend-expansion-log-retention/08-RESEARCH.md

<interfaces>
<!-- Key code structures the executor needs. Extracted from research. -->

From web/api/legacy.php (lines 2225-2230) - $weeksMap:
```php
$weeksMap = [
    '4weeks' => 4,
    '12weeks' => 12,
    '26weeks' => 26,
    '52weeks' => 52
];
```

From web/api/legacy.php - getBusinessUnitTrendData():
- Line 556: `$weekNum - 11` (go back 12 weeks; change to `$weekNum - 12` for 13 weeks)
- Line 568: `for ($i = 0; $i < 12; $i++)` (iteration count; change to 13)
- Lines 560-564: Year-boundary logic (`if ($startWeekNum < 1)`) -- already handles negative week numbers correctly

From web/api/legacy.php - getHistoricalTrend():
- Line 2217: `?? '12weeks'` default (change to `'13weeks'`)
- Line 2231: `?? 12` fallback (change to `13`)

From web/api/legacy.php - get_trend case:
- Line 3046: `?? '12weeks'` (change to `'13weeks'`)

From web/assets/js/components/trend-slider.js:
- Line 53: `timeRange: context.timeRange || '4weeks'` (default; change to `'13weeks'`)
- Lines 149-153: Label map (add `'13weeks': '13 Weeks'`)
- Line 236: Button array `['4weeks', '12weeks', '26weeks', '52weeks']` (replace `'12weeks'` with `'13weeks'`)
- Line 619: Label map (add `'13weeks': '13 weeks'`)

From web/assets/js/core/app.js:
- Line 959: `"12-Week Trend"` label (change to `"13-Week Trend"`)
- Line 1325: `'12-Week Trend'` XLSX sheet name (change to `'13-Week Trend'`)
- Line 1504: `'anomalies in last 12 weeks'` -- DO NOT CHANGE (anomaly detection uses its own window)
- Line 1620: `'12-Week Trend'` detail panel heading (change to `'13-Week Trend'`)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Expand backend trend defaults to 13 weeks</name>
  <files>web/api/legacy.php</files>
  <read_first>
    - web/api/legacy.php (lines 550-570 for getBusinessUnitTrendData, lines 2215-2235 for getHistoricalTrend and $weeksMap, lines 3040-3050 for get_trend case)
    - .planning/phases/08-trend-expansion-log-retention/08-RESEARCH.md (Pattern 1 table for exact line references)
  </read_first>
  <action>
Modify web/api/legacy.php at 6 specific locations:

1. **getBusinessUnitTrendData() line 556**: Change `$weekNum - 11` to `$weekNum - 12` (this makes the function go back 13 weeks instead of 12; the year-boundary logic at lines 560-564 already handles negative week numbers)

2. **getBusinessUnitTrendData() line 568**: Change `for ($i = 0; $i < 12; $i++)` to `for ($i = 0; $i < 13; $i++)` (iterate 13 times instead of 12)

3. **$weeksMap (lines 2225-2230)**: Add `'13weeks' => 13` entry. Keep `'12weeks' => 12` for backward compatibility (existing URLs with `time_range=12weeks` must still work). Final map:
```php
$weeksMap = [
    '4weeks' => 4,
    '12weeks' => 12,
    '13weeks' => 13,
    '26weeks' => 26,
    '52weeks' => 52
];
```

4. **getHistoricalTrend() line 2217**: Change `?? '12weeks'` to `?? '13weeks'`

5. **$numWeeks fallback line 2231**: Change `?? 12` to `?? 13`

6. **get_trend case line 3046**: Change `?? '12weeks'` to `?? '13weeks'`

DO NOT change any other occurrences of "12" in the file. The anomaly detection and other features have their own time windows.
  </action>
  <verify>
    <automated>grep -n "13weeks" web/api/legacy.php | head -10 && grep -n "i < 13" web/api/legacy.php && grep -n "weekNum - 12" web/api/legacy.php</automated>
  </verify>
  <acceptance_criteria>
    - web/api/legacy.php contains `'13weeks' => 13` in $weeksMap
    - web/api/legacy.php contains `'12weeks' => 12` in $weeksMap (backward compat preserved)
    - web/api/legacy.php contains `$i < 13` in getBusinessUnitTrendData loop
    - web/api/legacy.php contains `$weekNum - 12` in getBusinessUnitTrendData
    - web/api/legacy.php contains `?? '13weeks'` at both getHistoricalTrend default and get_trend case
    - web/api/legacy.php contains `?? 13` as $numWeeks fallback
    - No changes to anomaly detection code (line 1504 area unchanged)
  </acceptance_criteria>
  <done>Backend PHP returns 13 weeks of trend data by default, $weeksMap includes 13weeks entry, all API defaults changed from 12weeks to 13weeks, backward compatibility preserved for 12weeks URLs</done>
</task>

<task type="auto">
  <name>Task 2: Update frontend labels and trend slider to 13-week defaults</name>
  <files>web/assets/js/core/app.js, web/assets/js/components/trend-slider.js</files>
  <read_first>
    - web/assets/js/core/app.js (lines 955-965, 1320-1330, 1500-1510, 1615-1625)
    - web/assets/js/components/trend-slider.js (lines 48-58, 145-160, 230-240, 615-625)
    - .planning/phases/08-trend-expansion-log-retention/08-RESEARCH.md (Pattern 2 table)
  </read_first>
  <action>
Modify web/assets/js/core/app.js at 3 locations (NOT line 1504):

1. **Line 959**: Change `"12-Week Trend"` to `"13-Week Trend"` (BU card mini-chart label)
2. **Line 1325**: Change `'12-Week Trend'` to `'13-Week Trend'` (XLSX export sheet name)
3. **Line 1620**: Change `'12-Week Trend'` to `'13-Week Trend'` (detail panel heading)

DO NOT change line 1504 `'anomalies in last 12 weeks'` -- this is anomaly detection with its own time window per research Pitfall 4.

Modify web/assets/js/components/trend-slider.js at 4 locations:

1. **Line 53**: Change `context.timeRange || '4weeks'` to `context.timeRange || '13weeks'` (default time range)
2. **Lines 149-153 area**: Add `'13weeks': '13 Weeks'` to the label mapping object
3. **Line 236**: Change button array from `['4weeks', '12weeks', '26weeks', '52weeks']` to `['4weeks', '13weeks', '26weeks', '52weeks']` (replace 12weeks with 13weeks in UI buttons since users select visually)
4. **Line 619 area**: Add `'13weeks': '13 weeks'` to the second label mapping object
  </action>
  <verify>
    <automated>grep -n "13-Week Trend" web/assets/js/core/app.js && grep -c "12-Week Trend" web/assets/js/core/app.js && grep -n "13weeks" web/assets/js/components/trend-slider.js</automated>
  </verify>
  <acceptance_criteria>
    - web/assets/js/core/app.js contains `"13-Week Trend"` at 3 locations (lines ~959, ~1325, ~1620)
    - web/assets/js/core/app.js contains zero occurrences of `"12-Week Trend"` (all 3 replaced)
    - web/assets/js/core/app.js still contains `anomalies in last 12 weeks` (unchanged)
    - web/assets/js/components/trend-slider.js contains `|| '13weeks'` as default timeRange
    - web/assets/js/components/trend-slider.js contains `'13weeks'` in button array
    - web/assets/js/components/trend-slider.js contains `'13weeks': '13 Weeks'` in label map
    - web/assets/js/components/trend-slider.js does NOT contain `'12weeks'` in the button array (replaced by 13weeks)
  </acceptance_criteria>
  <done>All frontend trend labels show "13-Week Trend", trend slider defaults to 13 weeks with 13weeks button replacing 12weeks in the UI, anomaly detection text left unchanged</done>
</task>

</tasks>

<verification>
- `grep -c "'13weeks'" web/api/legacy.php` returns at least 3 (weeksMap entry + 2 defaults)
- `grep -c "13-Week Trend" web/assets/js/core/app.js` returns 3
- `grep "13weeks" web/assets/js/components/trend-slider.js | wc -l` returns at least 4
- `grep "'12weeks'" web/api/legacy.php` still returns at least 1 (backward compat in weeksMap)
- `grep "anomalies in last 12 weeks" web/assets/js/core/app.js` returns 1 (unchanged)
</verification>

<success_criteria>
- Backend API defaults to 13 weeks for all trend endpoints
- BU card mini-charts render 13 data points
- Trend slider opens with 13 weeks selected
- Trend slider buttons show 4, 13, 26, 52 week options
- All "12-Week Trend" labels updated to "13-Week Trend"
- Existing 12weeks API parameter still works (backward compatibility)
- Anomaly detection text unchanged
</success_criteria>

<output>
After completion, create `.planning/phases/08-trend-expansion-log-retention/08-01-SUMMARY.md`
</output>
