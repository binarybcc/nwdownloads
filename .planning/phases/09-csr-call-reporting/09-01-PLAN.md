---
phase: 09-csr-call-reporting
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - web/api/csr_report.php
  - web/csr_report.php
  - web/settings.php
autonomous: false
requirements:
  - CSR-01

must_haves:
  truths:
    - "Settings page displays a CSR Call Activity card with per-CSR outgoing call counts"
    - "Clicking the CSR card navigates to a dedicated report page"
    - "Report page shows a table with CSR Name, Outgoing, Received, Missed, Total columns plus a totals row"
    - "Report page shows a stacked bar chart of weekly call volume per CSR"
    - "CSR names are human-readable (Brittany Carroll, Chloe Welch), not raw codes (BC, CW)"
    - "Data covers a rolling 60-day window"
  artifacts:
    - path: "web/api/csr_report.php"
      provides: "CSR call data API with summary and weekly breakdown"
      exports: ["JSON response with summary, weekly, last_updated"]
    - path: "web/csr_report.php"
      provides: "Dedicated CSR report page with table and chart"
      min_lines: 100
    - path: "web/settings.php"
      provides: "CSR Call Activity card (5th card in grid)"
      contains: "csr_report.php"
  key_links:
    - from: "web/settings.php"
      to: "web/csr_report.php"
      via: "anchor tag href"
      pattern: "href=\"csr_report.php\""
    - from: "web/settings.php"
      to: "web/api/csr_report.php"
      via: "fetch on DOMContentLoaded"
      pattern: "fetch.*api/csr_report.php.*summary=true"
    - from: "web/csr_report.php"
      to: "web/api/csr_report.php"
      via: "fetch call for full data"
      pattern: "fetch.*api/csr_report.php"
    - from: "web/api/csr_report.php"
      to: "call_logs table"
      via: "PDO prepared statement"
      pattern: "FROM call_logs"
---

<objective>
Build CSR Call Reporting: an API endpoint for call data, a dedicated report page with table and chart, and a summary card on the settings page.

Purpose: Managers need to see how many outgoing calls each CSR has made to evaluate phone activity levels across the team.
Output: Three files -- `web/api/csr_report.php` (API), `web/csr_report.php` (report page), `web/settings.php` (modified with new card)
</objective>

<execution_context>
@/Users/user/.claude/get-shit-done/workflows/execute-plan.md
@/Users/user/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-csr-call-reporting/09-CONTEXT.md
@.planning/phases/09-csr-call-reporting/09-RESEARCH.md

<interfaces>
<!-- Database schema (call_logs table) -->
From database/migrations/014_add_call_logs_table.sql:
```sql
CREATE TABLE IF NOT EXISTS call_logs (
    id              BIGINT UNSIGNED AUTO_INCREMENT PRIMARY KEY,
    call_direction  ENUM('placed','received','missed') NOT NULL,
    call_timestamp  DATETIME NOT NULL,
    duration_sec    INT UNSIGNED DEFAULT 0,
    remote_number   VARCHAR(50) NOT NULL,
    phone_normalized CHAR(10) DEFAULT NULL,
    local_extension VARCHAR(20) NOT NULL DEFAULT '',
    source_group    VARCHAR(20) DEFAULT NULL COMMENT 'BC or CW (BroadWorks group)',
    raw_payload     TEXT DEFAULT NULL,
    imported_at     TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP,
    INDEX idx_source_group (source_group)
);
```

From web/api/legacy.php (DB connection pattern):
```php
$db_config = [
    'host' => getenv('DB_HOST') ?: 'localhost',
    'port' => getenv('DB_PORT') ?: 3306,
    'database' => getenv('DB_NAME') ?: 'circulation_dashboard',
    'username' => getenv('DB_USER') ?: 'circ_dash',
    'password' => getenv('DB_PASSWORD'),
    'socket' => getenv('DB_SOCKET') !== false ? getenv('DB_SOCKET') : '/run/mysqld/mysqld10.sock',
];

function connectDB(array $config): PDO {
    if (empty($config['socket']) || !file_exists($config['socket'])) {
        $dsn = "mysql:host={$config['host']};port={$config['port']};dbname={$config['database']};charset=utf8mb4";
    } else {
        $dsn = "mysql:unix_socket={$config['socket']};dbname={$config['database']};charset=utf8mb4";
    }
    $pdo = new PDO($dsn, $config['username'], $config['password']);
    $pdo->setAttribute(PDO::ATTR_ERRMODE, PDO::ERRMODE_EXCEPTION);
    return $pdo;
}
```

From web/settings.php (card order):
1. Rate Management (blue, `<a>` link)
2. File Processing (purple, `<a>` link)
3. Cache Management (green, `<div>` interactive)
4. Backup & Restore (orange, `<a>` link)
5. [NEW: CSR Call Activity card goes here -- teal, `<a>` link]
6. User Management (coming soon placeholder)
7. Dashboard Preferences (coming soon placeholder)
...remaining placeholders
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create CSR report API endpoint</name>
  <files>web/api/csr_report.php</files>
  <read_first>
    - web/api/legacy.php (DB connection pattern and connectDB function)
    - web/api/cache_management.php (API endpoint pattern with auth_check, JSON headers)
    - database/migrations/014_add_call_logs_table.sql (call_logs schema)
    - web/fetch_call_logs.php (source_group values BC/CW)
  </read_first>
  <action>
Create `web/api/csr_report.php` -- a read-only JSON API endpoint with two modes.

**File structure:**
1. `require_once __DIR__ . '/../auth_check.php';` (same as cache_management.php)
2. Set `Content-Type: application/json` header
3. Copy the `$db_config` array and `connectDB()` function from `web/api/legacy.php` verbatim
4. Define CSR name mapping array:
   ```php
   $csrNames = [
       'BC' => 'Brittany Carroll',
       'CW' => 'Chloe Welch',
   ];
   ```
5. Define `mapCsrName(string $group, array $csrNames): string` that returns `$csrNames[$group] ?? "Unknown ({$group})"`
6. Check `$_GET['summary']` to determine response mode

**Summary mode** (`?summary=true`):
Run this SQL via PDO prepared statement (no string concatenation):
```sql
SELECT
    COALESCE(source_group, 'UNKNOWN') AS source_group,
    SUM(CASE WHEN call_direction = 'placed' THEN 1 ELSE 0 END) AS placed,
    SUM(CASE WHEN call_direction = 'received' THEN 1 ELSE 0 END) AS received,
    SUM(CASE WHEN call_direction = 'missed' THEN 1 ELSE 0 END) AS missed,
    COUNT(*) AS total
FROM call_logs
WHERE call_timestamp >= DATE_SUB(CURDATE(), INTERVAL 60 DAY)
GROUP BY COALESCE(source_group, 'UNKNOWN')
ORDER BY source_group
```
Map each row's `source_group` through `mapCsrName()` to add a `name` field.

**Full mode** (no `summary` param):
Run both the summary query above AND this weekly breakdown query:
```sql
SELECT
    YEARWEEK(call_timestamp, 3) AS yw,
    DATE_FORMAT(
        DATE_SUB(call_timestamp, INTERVAL WEEKDAY(call_timestamp) DAY),
        '%Y-%m-%d'
    ) AS week_start,
    COALESCE(source_group, 'UNKNOWN') AS source_group,
    SUM(CASE WHEN call_direction = 'placed' THEN 1 ELSE 0 END) AS placed,
    SUM(CASE WHEN call_direction = 'received' THEN 1 ELSE 0 END) AS received,
    SUM(CASE WHEN call_direction = 'missed' THEN 1 ELSE 0 END) AS missed
FROM call_logs
WHERE call_timestamp >= DATE_SUB(CURDATE(), INTERVAL 60 DAY)
GROUP BY yw, week_start, COALESCE(source_group, 'UNKNOWN')
ORDER BY yw, source_group
```

**Last updated timestamp** (both modes):
```sql
SELECT MAX(imported_at) AS last_updated FROM call_logs
```

**Response shape (both modes):**
```json
{
  "success": true,
  "summary": [{"name": "Brittany Carroll", "group": "BC", "placed": 42, "received": 15, "missed": 3, "total": 60}],
  "weekly": [...],  // only in full mode, empty array in summary mode
  "last_updated": "2026-03-21 14:30:00"
}
```

**Error handling:** Wrap in try/catch. On exception, return `{"success": false, "error": "message"}` with HTTP 500.

**Empty data:** Return empty arrays (not null) for `summary` and `weekly` when no rows match. Return `null` for `last_updated` if no call_logs exist at all.
  </action>
  <verify>
    <automated>php -l web/api/csr_report.php</automated>
  </verify>
  <acceptance_criteria>
    - web/api/csr_report.php exists and passes `php -l` (no syntax errors)
    - File contains `require_once __DIR__ . '/../auth_check.php'`
    - File contains `'BC' => 'Brittany Carroll'` and `'CW' => 'Chloe Welch'`
    - File contains `Unknown ({$group})` fallback for unmapped codes
    - File contains `DATE_SUB(CURDATE(), INTERVAL 60 DAY)` in SQL
    - File contains `COALESCE(source_group, 'UNKNOWN')` in SQL
    - File contains `YEARWEEK(call_timestamp, 3)` in weekly query
    - File contains `$_GET['summary']` check for mode selection
    - File contains `json_encode` with `success`, `summary`, `weekly`, `last_updated` keys
    - No string concatenation in SQL -- uses PDO query() or prepare()/execute()
    - File contains `connectDB` function matching legacy.php pattern
  </acceptance_criteria>
  <done>API endpoint returns CSR call summary and weekly breakdown as JSON from the call_logs table with 60-day window, CSR name mapping, and unknown code fallback</done>
</task>

<task type="auto">
  <name>Task 2: Create CSR report page and add settings card</name>
  <files>web/csr_report.php, web/settings.php</files>
  <read_first>
    - web/settings.php (current card grid, Cache Management pattern for API-loaded stats, card HTML structure)
    - web/api/csr_report.php (the API just created in Task 1 -- response shape)
    - docs/DESIGN-SYSTEM.md (component patterns, page structure)
    - web/backup.php (example of standalone page with back-arrow header pattern)
  </read_first>
  <action>
**Part A: Create `web/csr_report.php` -- dedicated report page**

Page structure (follow existing page pattern from backup.php):
1. `require_once 'auth_check.php'` and `require_once 'version.php'`
2. HTML doctype, charset, viewport meta
3. Title: "CSR Call Activity - Circulation Dashboard"
4. Tailwind CSS link: `<link rel="stylesheet" href="assets/output.css?v=20251206">`
5. Chart.js CDN: `<script src="https://cdn.jsdelivr.net/npm/chart.js@4.4.0/dist/chart.umd.min.js"></script>`
6. Inter font import (same `<style>` block as settings.php)
7. Body with `bg-gray-50` class

**Header:** Same pattern as settings.php header but:
- Back arrow links to `settings.php` (not `index.php`)
- Title: "CSR Call Activity" with a teal phone icon (SVG path for phone/headset)
- Subtitle text: "Outgoing, received, and missed calls per CSR -- rolling 60-day window"

**Main content** (`max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-8`):

1. **"Data as of" line:** `<p id="lastUpdated" class="text-sm text-gray-500 mb-6">Loading...</p>` -- updated by JS with `last_updated` from API

2. **Summary table container** with skeleton loading:
   - `<div id="tableSkeleton">` with 3 `animate-pulse` rows (gray bars)
   - `<div id="tableContainer" class="hidden">` with the real table
   - Table classes: `min-w-full divide-y divide-gray-200` inside `overflow-x-auto bg-white rounded-lg shadow`
   - Table header: CSR Name | Outgoing | Received | Missed | Total (use `bg-gray-50 text-left text-xs font-medium text-gray-500 uppercase tracking-wider` for th)
   - Table body: populated by JS from `summary` array
   - Totals row at bottom: bold, `bg-gray-50` background, sums of each column
   - Empty state: if summary array is empty, show "No call data available for the last 60 days" message instead of table

3. **Chart container:**
   - Section heading: `<h2 class="text-lg font-semibold text-gray-900 mt-8 mb-4">Weekly Call Volume</h2>`
   - `<div id="chartSkeleton" class="animate-pulse h-64 bg-gray-200 rounded-lg"></div>`
   - `<div id="chartContainer" class="hidden"><canvas id="csrChart"></canvas></div>`
   - Empty state: if weekly array is empty, show "No weekly data available" text

**JavaScript (inline `<script>` at bottom, same pattern as settings.php):**

1. On `DOMContentLoaded`, fetch `api/csr_report.php` (no params = full mode)
2. On success:
   - Update `#lastUpdated` text to `Data as of: {last_updated}` (or "No data imported yet" if null)
   - Hide `#tableSkeleton`, show `#tableContainer`
   - Build table body rows from `summary` array: each row has `name`, `placed`, `received`, `missed`, `total`
   - Compute totals row by summing placed/received/missed/total across all summary entries
   - Hide `#chartSkeleton`, show `#chartContainer`
   - Build Chart.js stacked bar chart (details below)
3. On error: show error message in place of skeleton

**Chart.js configuration:**
- Type: `bar`
- Grouping: by week (X axis = week labels like "Mar 3", "Mar 10"), CSRs side-by-side within each week
- For each CSR, create 3 datasets (placed/received/missed) all sharing the same `stack` value (the CSR's group code)
- Colors per direction (same for all CSRs):
  - Placed: `rgba(59, 130, 246, 0.7)` (blue)
  - Received: `rgba(34, 197, 94, 0.7)` (green)
  - Missed: `rgba(239, 68, 68, 0.5)` (red, lighter)
- To distinguish CSRs visually, label datasets as "{CSR Name} - Placed", "{CSR Name} - Received", "{CSR Name} - Missed"
- Options: `scales.x.stacked = true`, `scales.y.stacked = true, beginAtZero: true`
- Responsive: true
- Week labels: format `week_start` dates as "Mon D" (e.g., "Mar 3") using JS `toLocaleDateString('en-US', {month: 'short', day: 'numeric'})`

**Footer:** Same pattern as settings.php (version string, copyright)

---

**Part B: Modify `web/settings.php` -- add CSR Call Activity card**

Insert a new `<a>` card AFTER the Backup & Restore card (line ~198, before the User Management placeholder). The card must be the 5th card in the grid.

Card HTML (follow the Rate Management / Backup & Restore `<a>` card pattern exactly):
```html
<!-- CSR Call Activity Card -->
<a href="csr_report.php" id="csrCard" class="settings-card bg-white rounded-lg shadow-md p-6 cursor-pointer border-2 border-transparent hover:border-teal-200">
    <div class="flex items-start justify-between mb-4">
        <div class="flex items-center gap-3">
            <div class="w-12 h-12 bg-teal-100 rounded-lg flex items-center justify-center">
                <svg class="w-6 h-6 text-teal-600" fill="none" stroke="currentColor" viewBox="0 0 24 24" aria-hidden="true">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M3 5a2 2 0 012-2h3.28a1 1 0 01.948.684l1.498 4.493a1 1 0 01-.502 1.21l-2.257 1.13a11.042 11.042 0 005.516 5.516l1.13-2.257a1 1 0 011.21-.502l4.493 1.498a1 1 0 01.684.949V19a2 2 0 01-2 2h-1C9.716 21 3 14.284 3 6V5z"></path>
                </svg>
            </div>
        </div>
        <div id="csrCallCount" class="inline-flex items-center px-2 py-0.5 rounded text-xs font-semibold bg-gray-100 text-gray-600">
            Loading...
        </div>
    </div>
    <h3 class="text-lg font-semibold text-gray-900 mb-2">CSR Call Activity</h3>
    <p class="text-sm text-gray-600 mb-4">
        View outgoing, received, and missed call counts per CSR over the last 60 days.
    </p>
    <div id="csrSummaryStats" class="text-xs text-gray-500 mb-4">
        <div class="flex justify-between mb-1">
            <span>Loading stats...</span>
        </div>
    </div>
    <div class="flex items-center gap-2 text-xs text-teal-600 font-medium">
        <span>View Full Report</span>
        <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24" aria-hidden="true">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M13 7l5 5m0 0l-5 5m5-5H6"></path>
        </svg>
    </div>
</a>
```

Add JavaScript to the existing `<script>` block in settings.php (after the cache management code, before the closing `</script>` tag):

```javascript
// Load CSR call stats on page load
async function loadCsrStats() {
    try {
        const response = await fetch('api/csr_report.php?summary=true');
        const data = await response.json();
        if (data.success && data.summary.length > 0) {
            const totalOutgoing = data.summary.reduce((sum, csr) => sum + csr.placed, 0);
            // Update badge with total outgoing count
            const badge = document.getElementById('csrCallCount');
            badge.className = 'inline-flex items-center px-2 py-0.5 rounded text-xs font-semibold bg-teal-100 text-teal-800';
            badge.textContent = totalOutgoing + ' outgoing';
            // Update summary stats with per-CSR outgoing counts
            const statsDiv = document.getElementById('csrSummaryStats');
            statsDiv.innerHTML = data.summary.map(csr =>
                `<div class="flex justify-between mb-1"><span>${csr.name}:</span><span>${csr.placed} placed</span></div>`
            ).join('');
        } else {
            document.getElementById('csrCallCount').textContent = 'No data';
            document.getElementById('csrSummaryStats').innerHTML = '<div class="text-gray-400">No call data available</div>';
        }
    } catch (error) {
        console.error('Failed to load CSR stats:', error);
        document.getElementById('csrCallCount').textContent = 'Error';
    }
}
```

Add `loadCsrStats()` call to the existing `DOMContentLoaded` listener, or add a new one if the existing pattern calls `loadCacheStats` directly.
  </action>
  <verify>
    <automated>php -l web/csr_report.php && php -l web/settings.php && grep -c "csr_report.php" web/settings.php</automated>
  </verify>
  <acceptance_criteria>
    - web/csr_report.php exists and passes `php -l`
    - web/csr_report.php contains `require_once 'auth_check.php'`
    - web/csr_report.php contains `require_once 'version.php'`
    - web/csr_report.php contains `chart.js@4.4.0` CDN script tag
    - web/csr_report.php contains `fetch('api/csr_report.php')` (without summary param)
    - web/csr_report.php contains `type: 'bar'` for Chart.js configuration
    - web/csr_report.php contains `stacked: true` in chart options
    - web/csr_report.php contains `rgba(59, 130, 246` (blue for placed)
    - web/csr_report.php contains `rgba(34, 197, 94` (green for received)
    - web/csr_report.php contains `rgba(239, 68, 68` (red for missed)
    - web/csr_report.php contains `tableSkeleton` and `chartSkeleton` IDs
    - web/csr_report.php contains `settings.php` in back-arrow href
    - web/csr_report.php contains `No call data available` empty state text
    - web/settings.php contains `href="csr_report.php"` on the CSR card
    - web/settings.php contains `hover:border-teal-200` (teal theme)
    - web/settings.php contains `bg-teal-100` (teal icon background)
    - web/settings.php contains `CSR Call Activity` card title
    - web/settings.php contains `api/csr_report.php?summary=true` fetch call
    - web/settings.php contains `csrCallCount` and `csrSummaryStats` element IDs
    - web/settings.php CSR card appears AFTER Backup & Restore card and BEFORE User Management placeholder
    - web/settings.php passes `php -l`
  </acceptance_criteria>
  <done>Report page renders summary table with all call directions plus totals row, stacked bar chart of weekly volume per CSR, and settings page has a teal CSR card with per-CSR outgoing counts that links to the report</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify CSR reporting end-to-end</name>
  <files>web/settings.php, web/csr_report.php, web/api/csr_report.php</files>
  <action>
Human verification of the complete CSR Call Activity feature. No automated work in this task -- the user visually inspects both the settings card and the report page to confirm correctness.
  </action>
  <what-built>CSR Call Activity reporting feature: API endpoint, dedicated report page with table and chart, and summary card on settings page</what-built>
  <how-to-verify>
    1. Navigate to the settings page (settings.php)
    2. Verify the CSR Call Activity card appears as the 5th card (after Backup & Restore, before Coming Soon placeholders)
    3. Verify the card shows per-CSR outgoing call counts (e.g., "Brittany Carroll: 42 placed")
    4. Verify the badge in the top-right shows total outgoing count (e.g., "57 outgoing")
    5. Click the CSR Call Activity card
    6. Verify the report page loads with a back arrow linking to settings
    7. Verify the summary table shows columns: CSR Name | Outgoing | Received | Missed | Total
    8. Verify a totals row exists at the bottom of the table
    9. Verify the stacked bar chart shows weekly call volume with blue/green/red segments
    10. Verify the "Data as of" timestamp appears above the table
    11. Verify CSR names show as "Brittany Carroll" and "Chloe Welch" (not "BC" and "CW")
  </how-to-verify>
  <verify>User confirms visual correctness of settings card and report page</verify>
  <done>User approves the CSR reporting feature as working correctly</done>
  <resume-signal>Type "approved" or describe any issues</resume-signal>
</task>

</tasks>

<verification>
- `php -l web/api/csr_report.php` -- no syntax errors
- `php -l web/csr_report.php` -- no syntax errors
- `php -l web/settings.php` -- no syntax errors
- Settings page loads CSR card with outgoing counts
- Report page shows table + chart with real call_logs data
- CSR names are human-readable, not raw codes
</verification>

<success_criteria>
- CSR-01 satisfied: Settings page displays outgoing call count per CSR mapped from scraper phone lines for the last 60 days
- Report page extends beyond the requirement with received/missed breakdown and weekly trend chart
- All three files pass PHP lint checks
- User confirms visual correctness via checkpoint
</success_criteria>

<output>
After completion, create `.planning/phases/09-csr-call-reporting/09-01-SUMMARY.md`
</output>
